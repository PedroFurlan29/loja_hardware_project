spring:
  datasource:
    hikari:
      initialization-fail-timeout: 30000
    url: jdbc:postgresql://localhost:5432/hardware_store_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    enabled: false

jwt:
  secret: this-is-a-32-byte-minimum-secret-for-jjwt-hs256
  expiration: 86400000
